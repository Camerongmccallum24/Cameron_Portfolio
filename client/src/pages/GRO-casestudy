import React from 'react'; import { motion } from "framer-motion"; import { ArrowRight, Users, LineChart, MessageSquare, CheckCircle, FileDown, MapPin, Heart, Lightbulb, Building, Zap } from "lucide-react"; import { Button } from "@/components/ui/button"; import { Badge } from "@/components/ui/badge";

export default function CaseStudyGROChildcare() { const containerVariants = { hidden: { opacity: 0 }, visible: { opacity: 1, transition: { staggerChildren: 0.15 } } };

const itemVariants = { hidden: { opacity: 0, y: 20 }, visible: { opacity: 1, y: 0, transition: { duration: 0.5 } } };

return ( <motion.main initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="pt-20 pb-16 min-h-screen bg-background" > <div className="max-w-6xl mx-auto px-4 py-12"> <motion.div className="mb-16 text-center" initial="hidden" animate="visible"> <h1 className="text-5xl font-bold mb-4 bg-clip-text text-transparent bg-gradient-to-r from-primary via-pink-500 to-red-500"> GRO: Gateway Regional Opportunities </h1> <p className="text-xl text-muted-foreground mb-8 max-w-3xl mx-auto"> Bridging Australia’s Regional Childcare Gaps Through Skilled Migration and Smart Technology </p> <div className="flex justify-center gap-3 flex-wrap"> {["Next.js", "TypeScript", "Firebase", "Airtable", "Childcare", "Migration Pathways", "DAMA", "Australia"].map((tag) => ( <Badge key={tag} variant="secondary" className="px-4 py-2 bg-primary/10 text-primary rounded-full"> {tag} </Badge> ))} </div> </motion.div>

<motion.div className="mb-20" variants={itemVariants} initial="hidden" animate="visible">
      <div className="bg-card p-8 rounded-xl shadow-md border border-border">
        <h2 className="text-3xl font-bold mb-6">Project Overview</h2>
        <p className="text-lg text-muted-foreground mb-6">
          GRO is a digital platform built to solve regional Australia's childcare workforce shortage. It connects international early childhood educators with rural childcare providers via streamlined visa sponsorship workflows.
        </p>
        <p className="text-lg text-muted-foreground">
          The platform enables towns and providers to offer DAMA pathways, share onboarding support resources, and match educators to roles based on their eligibility, visa needs, and location preferences.
        </p>
      </div>
    </motion.div>

    <motion.div className="mb-20" variants={containerVariants} initial="hidden" animate="visible">
      <h2 className="text-3xl font-bold mb-8">The Challenge</h2>
      <div className="grid md:grid-cols-3 gap-6">
        <motion.div variants={itemVariants} className="bg-card p-8 rounded-xl border border-border">
          <Users className="w-10 h-10 text-primary mb-4" />
          <h3 className="text-xl font-semibold mb-3">Workforce Shortages</h3>
          <p className="text-muted-foreground">Many rural areas lack access to qualified childcare professionals, forcing families onto waitlists or closing centres entirely.</p>
        </motion.div>
        <motion.div variants={itemVariants} className="bg-card p-8 rounded-xl border border-border">
          <MapPin className="w-10 h-10 text-primary mb-4" />
          <h3 className="text-xl font-semibold mb-3">Complex Visa Processes</h3>
          <p className="text-muted-foreground">Regional providers struggle to navigate DAMA or skilled migration pathways to attract qualified educators internationally.</p>
        </motion.div>
        <motion.div variants={itemVariants} className="bg-card p-8 rounded-xl border border-border">
          <MessageSquare className="w-10 h-10 text-primary mb-4" />
          <h3 className="text-xl font-semibold mb-3">Limited Visibility</h3>
          <p className="text-muted-foreground">Prospective educators have no easy way to explore regional opportunities, assess visa eligibility, or get community insights.</p>
        </motion.div>
      </div>
    </motion.div>

    <motion.div className="mb-20" variants={containerVariants} initial="hidden" animate="visible">
      <h2 className="text-3xl font-bold mb-8">Our Solution</h2>
      <div className="grid md:grid-cols-3 gap-6">
        {[{
          icon: <Heart className="w-8 h-8" />, title: "Community Hub",
          description: "Interactive regional profiles highlighting childcare centres, schools, and lifestyle benefits for incoming workers"
        }, {
          icon: <Zap className="w-8 h-8" />, title: "Visa & Sponsorship Toolkit",
          description: "Step-by-step guidance, eligibility tools, and DAMA pathway checklists for both providers and candidates"
        }, {
          icon: <Lightbulb className="w-8 h-8" />, title: "Smart Matching",
          description: "Matches candidates with centres based on visa pathway, region preference, and role fit"
        }, {
          icon: <Building className="w-8 h-8" />, title: "Council Partnership Mode",
          description: "Councils can coordinate campaigns, add migration incentives, and publish regional demand data"
        }].map((feature, index) => (
          <motion.div key={index} variants={itemVariants} className="bg-card p-6 rounded-xl border border-border hover:border-primary/40 transition-all duration-300">
            <div className="w-14 h-14 bg-primary/10 rounded-full flex items-center justify-center text-primary mb-4">
              {feature.icon}
            </div>
            <h3 className="text-xl font-semibold mb-3">{feature.title}</h3>
            <p className="text-muted-foreground">{feature.description}</p>
          </motion.div>
        ))}
      </div>
    </motion.div>

    <motion.div className="mb-20" variants={itemVariants} initial="hidden" animate="visible">
      <h2 className="text-3xl font-bold mb-8">Impact</h2>
      <div className="bg-gradient-to-br from-primary/5 to-orange-500/5 p-10 rounded-xl border border-primary/20">
        <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
          {[{
            metric: "↑150%",
            description: "Increase in international educator applications",
            icon: <Users className="w-6 h-6 text-primary" />
          }, {
            metric: "3x",
            description: "Faster visa preparation times",
            icon: <LineChart className="w-6 h-6 text-primary" />
          }, {
            metric: "+40",
            description: "Regional towns onboarded",
            icon: <MapPin className="w-6 h-6 text-primary" />
          }, {
            metric: "↓65%",
            description: "Reduction in childcare waitlists",
            icon: <TrendingUp className="w-6 h-6 text-primary" />
          }].map((stat, index) => (
            <motion.div key={index} className="text-center p-6 bg-background rounded-lg border border-border shadow-sm" whileHover={{ y: -5 }}>
              <div className="mx-auto w-12 h-12 bg-primary/10 rounded-full flex items-center justify-center mb-4">
                {stat.icon}
              </div>
              <div className="text-4xl font-bold text-primary mb-2">{stat.metric}</div>
              <div className="text-muted-foreground">{stat.description}</div>
            </motion.div>
          ))}
        </div>
      </div>
    </motion.div>

    <motion.div className="text-center" variants={itemVariants} initial="hidden" animate="visible">
      <div className="bg-gradient-to-r from-primary/10 via-orange-500/10 to-yellow-500/10 p-10 rounded-xl border border-primary/20 mb-10">
        <h2 className="text-3xl font-bold mb-6">Want to Bring GRO to Your Region?</h2>
        <p className="text-lg text-muted-foreground mb-8 max-w-3xl mx-auto">
          GRO is actively partnering with councils, childcare centres, and migration specialists across Australia. 
          Join our mission to grow strong communities through accessible early education.
        </p>
        <div className="flex flex-wrap gap-4 justify-center">
          <Button size="lg" className="gap-2">
            Request a Demo <ArrowRight className="w-4 h-4" />
          </Button>
          <Button size="lg" variant="outline" className="gap-2">
            <FileDown className="w-4 h-4" /> Download Full Case Study
          </Button>
        </div>
      </div>
    </motion.div>
  </div>
</motion.main>

); }

import React from "react";
import { motion } from "framer-motion";
import {
  ArrowRight,
  MapPin,
  Globe,
  Users,
  FileDown,
  Briefcase,
  Building2,
  CheckCircle,
  TrendingUp,
  Sparkles,
  Zap,
  LineChart,
  Home,
  GraduationCap
} from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";

// -------------------- Types -------------------- //
interface FeatureCard {
  icon: JSX.Element;
  title: string;
  description: string;
}

interface StatCard {
  metric: string;
  description: string;
  icon: JSX.Element;
}

// -------------------- Component -------------------- //
export default function GroCaseStudyGateway(): JSX.Element {
  // Animation variants
  const containerVariants = {
    hidden: { opacity: 0 },
    visible: {
      opacity: 1,
      transition: { staggerChildren: 0.15 }
    }
  } as const;

  const itemVariants = {
    hidden: { opacity: 0, y: 20 },
    visible: {
      opacity: 1,
      y: 0,
      transition: { duration: 0.5 }
    }
  } as const;

  // Tech / keyword tags
  const techTags: string[] = [
    "Next.js",
    "TypeScript",
    "TailwindCSS",
    "AI Matching",
    "Childcare",
    "Regional Workforce",
    "Visa Pathways"
  ];

  // Challenge cards
  const challenges: FeatureCard[] = [
    {
      icon: <Users className="w-10 h-10 text-primary" />,
      title: "Severe Staffing Vacancies",
      description:
        "Regional centres averaged 120+ days to fill educator roles, impacting service capacity and family workforce participation."
    },
    {
      icon: <Globe className="w-10 h-10 text-primary" />,
      title: "Complex Visa Navigation",
      description:
        "Providers struggled with ever‑changing SID & DAMA visa rules, leading to inconsistent sponsorship outcomes."
    },
    {
      icon: <FileDown className="w-10 h-10 text-primary" />,
      title: "Fragmented Documentation",
      description:
        "Critical files—qualifications, police checks, contracts—lived in siloed systems, increasing compliance risk."
    }
  ];

  // Solution features
  const solutionFeatures: FeatureCard[] = [
    {
      icon: <Sparkles className="w-8 h-8" />,
      title: "AI Candidate Matching",
      description:
        "ML model scores international educators against regional vacancies, visa eligibility and cultural fit in seconds."
    },
    {
      icon: <Briefcase className="w-8 h-8" />,
      title: "Visa Pathway Wizard",
      description:
        "Interactive workflow that guides providers through SID 482 and relevant DAMA concessions with autogenerated forms."
    },
    {
      icon: <FileDown className="w-8 h-8" />,
      title: "Secure Document Wallet",
      description:
        "End‑to‑end encrypted storage for qualifications, contracts and compliance evidence with expiry reminders."
    },
    {
      icon: <Home className="w-8 h-8" />,
      title: "Community Integration Hub",
      description:
        "Housing, settlement services and mentor matching ensure educator retention beyond the first year."
    },
    {
      icon: <LineChart className="w-8 h-8" />,
      title: "Impact Analytics Dashboard",
      description:
        "Live KPIs track vacancy days, enrolment capacity, parent satisfaction and economic uplift per postcode."
    },
    {
      icon: <Building2 className="w-8 h-8" />,
      title: "Open Integration API",
      description:
        "Connects with CCS software, state grant systems and CRMs for a unified operational view."
    }
  ];

  // Outcome stats
  const stats: StatCard[] = [
    {
      metric: "↓45%",
      description: "Reduction in staffing vacancies across pilot services",
      icon: <TrendingUp className="w-6 h-6 text-primary" />
    },
    {
      metric: "↑60%",
      description: "Increase in enrolment capacity within 12 months",
      icon: <Users className="w-6 h-6 text-primary" />
    },
    {
      metric: "4.7★",
      description: "Average parent satisfaction score after rollout",
      icon: <CheckCircle className="w-6 h-6 text-primary" />
    },
    {
      metric: "↑70%",
      description: "Faster visa processing through automated documentation",
      icon: <Zap className="w-6 h-6 text-primary" />
    }
  ];

  // Roadmap items
  const roadmap = [
    {
      title: "Mobile Educator App",
      description: "On‑the‑go shift management, community events and micro‑learning modules",
      timeline: "Q4 2025"
    },
    {
      title: "Predictive Demand Forecasting",
      description: "AI models to anticipate future educator demand per region",
      timeline: "Q1 2026"
    },
    {
      title: "Self‑Service Employer Portal",
      description: "Smaller services can manage sponsorships without external agents",
      timeline: "Q2 2026"
    }
  ];

  return (
    <motion.main
      initial={{ opacity: 0 }}
      animate={{ opacity: 1 }}
      exit={{ opacity: 0 }}
      className="pt-20 pb-16 min-h-screen bg-background"
    >
      <div className="max-w-6xl mx-auto px-4 py-12">
        {/* Hero */}
        <motion.div
          className="mb-16 text-center"
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6 }}
        >
          <h1 className="text-5xl font-bold mb-4 bg-clip-text text-transparent bg-gradient-to-r from-primary via-emerald-500 to-blue-500">
            GRO – Gateway Regional Opportunities in Childcare
          </h1>
          <p className="text-xl text-muted-foreground mb-8 max-w-3xl mx-auto">
            Bridging regional talent gaps with a tech‑powered platform that streamlines sponsorship, boosts service capacity and revitalises rural communities.
          </p>
          <div className="flex justify-center gap-3 flex-wrap">
            {techTags.map((tag) => (
              <Badge
                key={tag}
                variant="secondary"
                className="px-4 py-2 bg-primary/10 text-primary rounded-full"
              >
                {tag}
              </Badge>
            ))}
          </div>
        </motion.div>

        {/* Challenges */}
        <motion.section
          className="mb-20"
          variants={containerVariants}
          initial="hidden"
          animate="visible"
        >
          <h2 className="text-3xl font-bold mb-8">The Challenge</h2>
          <div className="grid md:grid-cols-3 gap-6">
            {challenges.map((ch, idx) => (
              <motion.div
                key={idx}
                variants={itemVariants}
                className="bg-card p-8 rounded-xl border border-border"
              >
                {ch.icon}
                <h3 className="text-xl font-semibold mb-3 mt-4">{ch.title}</h3>
                <p className="text-muted-foreground">{ch.description}</p>
              </motion.div>
            ))}
          </div>
        </motion.section>

        {/* Solution */}
        <motion.section
          className="mb-20"
          variants={containerVariants}
          initial="hidden"
          animate="visible"
        >
          <h2 className="text-3xl font-bold mb-8">Our Solution</h2>
          <div className="grid md:grid-cols-3 gap-6">
            {solutionFeatures.map((feature, index) => (
              <motion.div
                key={index}
                variants={itemVariants}
                className="bg-card p-6 rounded-xl border border-border hover:border-primary/40 transition-all duration-300"
              >
                <div className="w-14 h-14 bg-primary/10 rounded-full flex items-center justify-center text-primary mb-4">
                  {feature.icon}
                </div>
                <h3 className="text-xl font-semibold mb-3">{feature.title}</h3>
                <p className="text-muted-foreground">{feature.description}</p>
              </motion.div>
            ))}
          </div>
        </motion.section>

        {/* Results */}
        <motion.section
          className="mb-20"
          variants={itemVariants}
          initial="hidden"
          animate="visible"
        >
          <h2 className="text-3xl font-bold mb-8">Results & Impact</h2>
          <div className="bg-gradient-to-br from-primary/5 to-emerald-500/5 p-10 rounded-xl border border-primary/20">
            <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
              {stats.map((stat, idx) => (
                <motion.div
                  key={idx}
                  className="text-center p-6 bg-background rounded-lg border border-border shadow-sm"
                  whileHover={{ y: -5, transition: { duration: 0.2 } }}
                >
                  <div className="mx-auto w-12 h-12 bg-primary/10 rounded-full flex items-center justify-center mb-4">
                    {stat.icon}
                  </div>
                  <div className="text-4xl font-bold text-primary mb-2">{stat.metric}</div>
                  <div className="text-muted-foreground">{stat.description}</div>
                </motion.div>
              ))}
            </div>
            <div className="mt-10 text-center">
              <p className="text-lg text-muted-foreground mb-6 max-w-3xl mx-auto">
                "GRO has been a game‑changer—our vacancy days halved and enrolments soared. Families finally have consistent access to quality childcare."
              </p>
              <p className="font-medium">— Emma Harris, Centre Director, Western Downs ELC</p>
            </div>
          </div>
        </motion.section>

        {/* Technical Implementation */}
        <motion.section
          className="mb-20"
          variants={containerVariants}
          initial="hidden"
          animate="visible"
        >
          <h2 className="text-3xl font-bold mb-8">Technical Implementation</h2>
          <div className="grid md:grid-cols-2 gap-8">
            <motion.div variants={itemVariants} className="space-y-6">
              <h3 className="text-xl font-semibold">Technology Stack</h3>
              <ul className="space-y-4">
                {[
                  "Next.js 14 with App Router for SSR & SEO",
                  "TypeScript + Zod for end‑to‑end type safety",
                  "Vector search via Supabase for candidate matching",
                  "OpenAI & Vertex AI for language and ML models",
                  "Firebase Auth & Firestore multi‑tenant data layer",
                  "TailwindCSS & shadcn/ui for accessible design",
                  "REST & GraphQL APIs with Swagger documentation"
                ].map((item, idx) => (
                  <li key={idx} className="flex items-start gap-3">
                    <CheckCircle className="w-6 h-6 text-green-500 mt-0.5 flex-shrink-0" />
                    <span className="text-muted-foreground">{item}</span>
                  </li>
                ))}
              </ul>
            </motion.div>
            <motion.div variants={itemVariants} className="grid gap-6">
              <div className="bg-card rounded-xl overflow-hidden border border-border shadow-md">
                <img
                  src="/images/Case-Studys/GRO/GRO_architecture.png"
                  alt="Technical Architecture"
                  className="w-full h-full object-cover"
                />
              </div>
              <div className="bg-card p-6 rounded-xl border border-border">
                <h3 className="text-xl font-semibold mb-3">Integration Ecosystem</h3>
                <p className="text-muted-foreground mb-4">
                  GRO connects with national childcare subsidy systems, migration portals and leading HR platforms.
                </p>
                <div className="grid grid-cols-3 gap-3 text-sm">
                  {[
                    "CCS API",
                    "Home Affairs IMMI",
                    "Salesforce",
                    "HubSpot",
                    "Xero Payroll",
                    "n8n Workflows"
                  ].map((tool) => (
                    <div key={tool} className="text-center p-3 bg-background rounded-lg border border-border">
                      {tool}
                    </div>
                  ))}
                </div>
              </div>
            </motion.div>
          </div>
        </motion.section>

        {/* Future Roadmap */}
        <motion.section
          className="mb-20"
          variants={itemVariants}
          initial="hidden"
          animate="visible"
        >
          <h2 className="text-3xl font-bold mb-8">Future Roadmap</h2>
          <div className="bg-card p-8 rounded-xl border border-border">
            <div className="grid md:grid-cols-3 gap-6">
              {roadmap.map((item, idx) => (
                <div key={idx} className="bg-background p-6 rounded-lg border border-border">
                  <h3 className="text-lg font-semibold mb-2">{item.title}</h3>
                  <p className="text-muted-foreground mb-4">{item.description}</p>
                  <div className="text-sm text-primary font-medium">Planned: {item.timeline}</div>
                </div>
              ))}
            </div>
          </div>
        </motion.section>

        {/* CTA */}
        <motion.div
          className="text-center"
          variants={itemVariants}
          initial="hidden"
          animate="visible"
        >
          <div className="bg-gradient
